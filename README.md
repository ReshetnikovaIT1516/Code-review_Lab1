** Задача 1 ** 
* Код был разделен на модули: Header.h, Source.cpp, и main.cpp
* Был использован using namespace std, заменено на std::
* Простая структура имела неправильное название Artifact, она должна иметь в названии перфикс F
* В структуре имена публичных полей начинались с маленькой буквы, исправлено на заглавные
* Также указан неточный размер int, исправлено на int32
* У этой функции неправильно стояла фигурная скобка, рядом с ней, а не на следующей строке
* Из функции main убран алгоритм для поиска оптимального набора артефактов в целях оптимизации
* Изначальный код не выводил номера выбранных артефактов, поэтому в алгоритм выше добавлен вектор для хранения текущих и итоговых индексов, и в цикл добавлена работа с ним
* Исправлено setlocale(0, "") на setlocale(LC_ALL, "")
* Исправлено непонятное название переменной для работы с файлом с f1 на InputFile
* Исправлено непонятное название переменной для работы с весом и объемом (n, b, sumw, sumv, k, maxw, maxv -> ArtifactCount, MaxTotalWeight, CurrentWeight, CurrentVolume, StartIndex, BestWeight, BestVolume)
* Добавлены проверки на корректность считывания из файла чисел веса, объема и тд
* Исправлено постфиксное i++ на префиксное
* Добавлен вывод номеров артефактов
** Задача 2
* Код был разделен на модули: Header.h, Source.cpp, и main.cpp
* Был использован using namespace std, заменено на std::
** Задача 3
Код был разделен на модули: Header.h, Source.cpp, и main.cpp
* Был использован using namespace std, заменено на std::

** Задача 4
* Код был разделен на модули: Header.h, Source.cpp, и main.cpp
* Был использован using namespace std, заменено на std::
